resolving wheels for python 3.11: computing uv closure for bundle `protomotions-deps-pack`: uv lock failed for bundle `protomotions-deps-pack` (python 3.11, linux-64):

Using CPython 3.11.15
  × No solution found when resolving dependencies for split (markers:
  │ python_full_version == '3.11.*' and platform_machine == 'x86_64' and
  │ sys_platform == 'linux'):
  ╰─▶ Because only the following versions of antlr4-python3-runtime are
      available:
          antlr4-python3-runtime<4.9.dev0
          antlr4-python3-runtime>=4.10.dev0
      and hydra-core==1.3.2 depends on antlr4-python3-runtime==4.9.*, we can
 conclude that hydra-core==1.3.2 cannot be used.
      And because your project depends on hydra-core==1.3.2, we can conclude
      that your project's requirements are unsatisfiable.

conflict attribution (conda constraint provenance):
  - package `antlr4-python3-runtime` requires `antlr4-python3-runtime<4.9.dev0` but conda pins `antlr4-python3-runtime==4.9.3` (conda package `antlr4-python3-runtime` ==4.9.3, from auto-route, env `default`)

hint: run `retread solve` to widen the offending conda pin.

