Error:   × failed to solve requirements of environment 'pm-isaaclab' for platform
  │ 'linux-64'
  ├─▶   × failed to communicate with the build backend (/oscar/data/
  │     │ stellex/glvov/imprint-env-test/_session_briefs/pixi-build-
  │     │ retread-bin)
  │     help: Ensure that the build backend implements the JSON-RPC
  │           protocol correctly.
  │
  ├─▶ ErrorObject { code: InvalidParams, message: "resolving wheels for python
  │   3.11: computing uv closure for bundle `protomotions-deps-pack`: uv
  │   lock failed for bundle `protomotions-deps-pack` (python 3.11, linux-
  │   64):\n\nUsing CPython 3.11.15\n  × No solution found when resolving
  │   dependencies for split (markers:\n  │ python_full_version == '3.11.*'
  │   and platform_machine == 'x86_64' and\n  │ sys_platform == 'linux'):
  │   \n  ╰─▶ Because only the following versions of antlr4-python3-runtime
  │   are\n      available:\n          antlr4-python3-runtime<4.9.dev0\n
  │   antlr4-python3-runtime==4.9\n          antlr4-python3-runtime==4.9.1\n
  │   antlr4-python3-runtime==4.9.2\n          antlr4-python3-runtime==4.9.3\n
  │   antlr4-python3-runtime>4.10.dev0\n      and antlr4-python3-
  │   runtime>=4.9,<=4.9.3 has no usable wheels, we can\n      conclude that
  │   antlr4-python3-runtime>=4.9,<=4.9.3 cannot be used. And because
  │   hydra-core==1.3.2 depends on antlr4-python3-runtime==4.9.*\n      and
  │   your project depends on hydra-core==1.3.2, we can conclude that\n
  │   your project's requirements are unsatisfiable.\n\nno generated conda
  │   constraint was named in uv's message; the conflict may be intrinsic
  │   to the PyPI requirements.\n\npackage antlr4-python3-runtime has no
  │   wheels and no conda candidate; options: allow build (no-build =
  │   false), drop-dep, or vendor a wheel.\n\nhint: run `retread solve` to
  │   widen the offending conda pin.\n", data: None }
  ╰─▶ ErrorObject { code: InvalidParams, message: "resolving wheels for python
      3.11: computing uv closure for bundle `protomotions-deps-pack`: uv
      lock failed for bundle `protomotions-deps-pack` (python 3.11, linux-
      64):\n\nUsing CPython 3.11.15\n  × No solution found when resolving
      dependencies for split (markers:\n  │ python_full_version == '3.11.*'
      and platform_machine == 'x86_64' and\n  │ sys_platform == 'linux'):
      \n  ╰─▶ Because only the following versions of antlr4-python3-runtime
      are\n      available:\n          antlr4-python3-runtime<4.9.dev0\n
      antlr4-python3-runtime==4.9\n          antlr4-python3-runtime==4.9.1\n
      antlr4-python3-runtime==4.9.2\n          antlr4-python3-runtime==4.9.3\n
      antlr4-python3-runtime>4.10.dev0\n      and antlr4-python3-
      runtime>=4.9,<=4.9.3 has no usable wheels, we can\n      conclude that
      antlr4-python3-runtime>=4.9,<=4.9.3 cannot be used. And because
      hydra-core==1.3.2 depends on antlr4-python3-runtime==4.9.*\n      and
      your project depends on hydra-core==1.3.2, we can conclude that\n
      your project's requirements are unsatisfiable.\n\nno generated conda
      constraint was named in uv's message; the conflict may be intrinsic
      to the PyPI requirements.\n\npackage antlr4-python3-runtime has no
      wheels and no conda candidate; options: allow build (no-build =
      false), drop-dep, or vendor a wheel.\n\nhint: run `retread solve` to
      widen the offending conda pin.\n", data: None }
